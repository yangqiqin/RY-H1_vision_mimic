# RY-H1(16) 灵巧手视觉动作模仿工程 —— 错误排查手册

> 本手册面向**实际操作中遇到的错误**，按"现象 → 可能原因 → 检查/调整步骤 → 代码或设备修改"组织。
> 遇到问题时**按顺序对照表格逐项排查**，从最简单的（电源/接线/版本）开始，直到问题解决。
>
> 依据：知识库《RY-H1(16)灵巧手用户使用手册》V1.0.0、《睿研灵巧的SDK入门指南》V1.01、
> 官方 demo、`README_1.md`（L515）、`lib/L515_driver.py`。

---

## 目录

- [A. 环境与依赖](#a-环境与依赖)
- [B. 灵巧手连接与控制](#b-灵巧手连接与控制)
- [C. L515 摄像头](#c-l515-摄像头)
- [D. 关节/电机控制异常](#d-关节电机控制异常)
- [E. 动作与安全](#e-动作与安全)
- [F. 综合排查流程](#f-综合排查流程)

---

## A. 环境与依赖

| # | 现象 | 可能原因 | 检查与调整 |
|---|---|---|---|
| A1 | `ModuleNotFoundError: numpy/cv2/mediapipe` | 依赖未装 | `pip install -r requirements.txt`（numpy, opencv-python, mediapipe 必须） |
| A1b | `AttributeError: module 'mediapipe' has no attribute 'solutions'` | **mediapipe 1.x 已移除 `mp.solutions.hands`** | 本工程已改用 Tasks API + `hand_landmarker.task`；确认版本 ≥0.10.14 |
| A1c | `未找到手部模型文件 hand_landmarker.task` | 新版 MediaPipe 需要模型文件 | 把 `hand_landmarker.task` 放**项目根目录**（自动搜索），或 `--hand-model 路径`；官方下载地址见 README/`vision.hand_pose.MODEL_DOWNLOAD_URL` |
| A2 | `ModuleNotFoundError: pyrealsense2` | L515 库未装 | `pip install pyrealsense2==2.54.2.5684` |
| A3 | `ModuleNotFoundError: serial` | RS485 库未装 | `pip install pyserial`（仅 RS485 需要） |
| A4 | `找不到 RyhandLibx64.dll` | SDK 库不在搜索路径 | 把 `RyhandLibx64.dll` 放**工程根目录**（`_load_lib` 搜「当前目录+根目录」） |
| A5 | `ModuleNotFoundError: PCANBasic` / `ControlCAN` | 驱动文件缺失 | 把 `lib/PCANBasic.py` / `ControlCAN.py/.dll` 放**工程根目录**（`transport._ensure_driver_path`） |
| A6 | 从子目录运行报错找不到包 | 路径引导缺失 | 入口文件顶部必须有 `sys.path.insert(0, 根目录)` + `os.chdir(根目录)`（见《修改记录文档》修改1）；**或从根目录运行**：`python -m gui.hand_gui` |
| A7 | 控制台中文/✓✗乱码或 `UnicodeEncodeError` | GBK 编码 | 入口文件顶部加 `sys.stdout.reconfigure(encoding="utf-8", errors="replace")` |
| A8 | `selfcheck.py` 报"有失败项" | 无硬件时部分项预期 FAIL | 只关注你关心的项：换算 [PASS] 即核心逻辑正常；硬件项（L515/摄像头/电机）FAIL 属预期 |

---

## B. 灵巧手连接与控制

| # | 现象 | 可能原因 | 检查与调整 |
|---|---|---|---|
| B1 | `test_hand` 显示 0/16 电机应答 | ①电源不足 ②波特率不符 ③接线错误 ④驱动未装 | ①电源必须 **24V/8A 以上**（手册：最大功率 100W，限流 8A+）；②CAN 波特率 **1Mbps**、RS485 **5Mbps**；③M9 航插对齐红点轻推、**禁止带电插拔**；④PCAN 驱动 / ControlCAN.dll 是否正确 |
| B2 | 控制时突然复位/回到初始位置 | 电源功率不足 / 接线不良 | 换 24V/8A+ 电源；检查线缆；**带载必须用 0xAA 位置速度电流模式**（本工程默认） |
| B3 | `RyCanServoBusInit` 失败 ret≠0 | Hook/Listen 空间不足 / 库加载异常 | 检查 `RyhandLibx64.dll` 是否加载；hook=40/listen=33 已配置；看日志 `[hand] 加载库失败` |
| B4 | 调用返回 250（`enServo_Fail`） | 伺服未响应/超时 | 检查电机 ID 是否正确、总线波特率、`usTimeout` 是否太小 |
| B5 | 返回 251（`enServo_ParamErr`） | 参数错误 | 检查角度是否超范围（侧摆±20°/近节0~90°/远节0~75°/16关节0~110°）；指令长度是否正确 |
| B6 | 返回 254（`enCanMsgSentFail`） | CAN 发送失败 | 检查通信连接（PCAN/485 线）、波特率、`transport` 日志 |
| B7 | 返回 255（`enLibHookApplyFailed`） | Hook 申请失败 | 增大 `hook_num`（当前 40）；检查总线是否已初始化 |
| B8 | 收不到主动上报/状态 | ①未开上报 ②Listen 的 ID 不对 | ①`RyParam_SetUpateRate(id, 20, ...)`；②Listen `ulId = 伺服ID + 256`，命令码 0xA0/0xAA（`AddListen` 32 个已配） |
| B9 | 读到旧数据/乱跳 | `ucConfidence==0`（数据不可信） | 读数据前先检查 `ucConfidence`；确认上报频率正常 |
| B10 | 手完全没反应但 CAN 有帧 | 电源/接线/波特率 | 按 B1 排查；`candump` 或 `candump can0`（Linux）看是否有 0xAA 指令帧发出 |

---

## C. L515 摄像头

| # | 现象 | 可能原因 | 检查与调整 |
|---|---|---|---|
| C1 | `pyrealsense2 版本 xx 不兼容 L515` | 装了 2.55+（L515 已被移除支持） | **必须** `pip install pyrealsense2==2.54.2.5684`（README_1.md 指明 Just Stable） |
| C2 | `0x80070005 拒绝访问 / Access denied` | Windows 相机隐私权限 / 非管理员 | **以管理员身份运行**终端/程序；设置→隐私→相机→允许应用访问 |
| C3 | `未找到 RealSense 设备` | ①USB/驱动 ②供电 ③线缆 | ①**USB3.0 直连**（勿经 USB2 集线器）；②装 Intel RealSense SDK 2.0 运行时；③换线/换口；④官方 RealSense Viewer 验证出图 |
| C4 | `RealSense 流启动失败`（分辨率组合） | 深度/RGB 分辨率不匹配 | 用 L515_driver.py 标准参数：**depth 1024×768 + RGB 1280×720 @30FPS**（本工程已默认） |
| C5 | `check_realsense_devices()` 返回 [] | 见 C1/C2/C3 | 先 `python apps/test_camera.py --list` 看日志具体是版本/权限/枚举哪一步失败 |
| C6 | 深度与 RGB 尺寸不一致告警 | 对齐失败 | 确认 `rs.align(rs.stream.color)` 已创建（本工程 `read()` 内自动对齐）；检查两流分辨率 |
| C7 | 深度图全黑/花屏 | 深度流未对齐/参数问题 | 用 `cv2.convertScaleAbs(depth, alpha=0.03)` 可视化（本工程已用）；确认深度流为 `z16` |
| C8 | L515 能用但很卡 | USB2 带宽 / 分辨率过高 | 确认 USB3 直连；必要时降分辨率（depth 640×480 + RGB 1280×720，README_1 例1） |

> **L515 驱动标准代码**（用户提供 `lib/L515_driver.py`，本工程已对齐）：
> ```python
> config.enable_stream(rs.stream.color, 1280, 720, rs.format.bgr8, 30)
> config.enable_stream(rs.stream.depth, 1024, 768, rs.format.z16, 30)
> align = rs.align(rs.stream.color)     # 深度对齐到彩色
> ```

---

## D. 关节/电机控制异常

| # | 现象 | 可能原因 | 检查与调整 |
|---|---|---|---|
| D1 | **滑"近节"滑条实际是侧摆** | **手型选反**（左手当右手） | GUI 点"**左手**"（`hand_lr=0`）立即生效；或 `--hand-lr 0`。左右手电机 1/2 镜像，共模(近节)/差模(侧摆)互换 |
| D2 | **某电机无响应（如 9 号中指远节）** | ①硬件/行程 ②接线 ③手型无关 | ①GUI 底部"**单电机测试**"：ID=9 位置=2048 → 直接对电机9发指令；②有动作→查关节换算/手型；无动作→查行程("**查行程**"按钮，应≈4095) |
| D3 | 行程 < 3000（幅度小/无响应） | 行程配置受限 | 上位机校准；或 `hand.set_stroke(mid, 4095)`（先 `get_stroke` 读当前值，谨慎） |
| D4 | 电机"过温保护/过流保护"很快出现 | 非 0xAA 模式带载 | **必须用 0xAA 位置速度电流控制**（本工程 `move_joints` 默认）；电流从 300~500mA 起步；**冷却 5 分钟再试**（手册 FAQ） |
| D5 | 抓取松垮/无力 | 0xAA 模式未启用 / 电流太小 | 确认 0xAA 模式（默认）；`--max-current` 从 300~500 起步 |
| D6 | 侧摆只能单向 | GUI 滑条范围错误（旧版） | 本工程已修复：侧摆滑条 **-20°~+20°** 双向；升级后重试 |
| D7 | 关节编号/顺序对不上 | 关节 ID 映射理解错误 | 关节 ID 1~16 = [拇指侧摆,近节,远节, 食指…, 小指远节, 第16关节]；电机映射见下 |
| D8 | 16 号关节（拇指横向旋转）幅度小 | 行程受限 / 换算系数 | 手册：**大拇指横向旋转 0~125°**（含大侧摆与小侧摆），demo 换算用 0~110°；用 `check_joint16_stroke()` 查行程 |
| D9 | 读关节角度异常 | 位域解析/手型 | `parse_servo_info` 已按 MFingerInfo_t 位域实现（cmd8/status8/P12/V12/I12/F12）；确认 `hand_lr` 与读侧摆取反逻辑 |

**电机 ↔ 关节映射**（官方 README）：

| 手指 | 电机 ID | 关节 ID（侧摆/近节/远节） |
|---|---|---|
| 大拇指 | 1,2,3 | 1,2,3 |
| 食指 | 4,5,6 | 4,5,6 |
| 中指 | 7,8,9 | 7,8,9 |
| 无名指 | 10,11,12 | 10,11,12 |
| 小指 | 13,14,15 | 13,14,15 |
| 第16关节 | 16 | 16（0~110°） |

> 关节9 = 中指远节 = **电机9（M3）**，只由电机3控制（与手型无关）。
> 若 GUI 滑 9 号无响应 → 先"单电机测试"验证电机9硬件，再查行程。

---

## E. 动作与安全

| # | 现象 | 可能原因 | 检查与调整 |
|---|---|---|---|
| E1 | 握拳时食指伸直/带侧摆 | 旧版预设（demo palm_bend 缺陷） | 本工程已重设计标准握拳（侧摆全0、所有手指弯曲）；升级后重试 |
| E2 | 动作方向相反 | 手型选反 / 视觉映射方向 | `--hand-lr` 与手型一致；`--scale 0.3` 起步；调 `hand_pose.py` 阈值/取反 |
| E3 | 动作幅度太大 | scale 未限幅 | **首次 `--scale 0.3~0.5`**；确认后再放大 |
| E4 | 动作抖动 | 视觉抖动/平滑不足 | 提高 `--smooth`（0.5+）或降低 `--rate` |
| E5 | MediaPipe 卡 | CPU 推理慢 | 降 `--rate`、低分辨率、用 GPU 机型 |
| E6 | 程序退出后手保持握紧 | 未复位 | 程序 finally 已 `open_hand()`；异常强杀时手动按"张开"或断电 |

**安全红线（手册必读）：**
1. 接线：24V 正负极确认无误再上电；**禁止带电插拔**；裸露线电工胶布包裹；
2. 供电：24V/8A+（手册：最大功率 100W，建议限流 8A 以上）；
3. 带载必须 0xAA 限流模式，电流从 300~500mA 起步，过温过流冷却 5 分钟；
4. **不要用力掰手指**（腱绳结构会崩线）；手指附近运行时不要站人。

---

## F. 综合排查流程

```
遇到问题
  │
  ├─ 先跑环境自检：python selfcheck.py
  │     ├─ 依赖 [FAIL] → A1~A3
  │     ├─ SDK 库 [FAIL] → A4
  │     ├─ 驱动文件 [FAIL] → A5
  │     ├─ L515 [FAIL] → C1~C4
  │     └─ 换算 [FAIL] → 检查 angles2motor（不应发生，逻辑已锁定）
  │
  ├─ 灵巧手问题
  │     ├─ test_hand.py 0/16 → B1（电源/波特率/接线/驱动）
  │     ├─ 近节/侧摆错乱 → D1（切手型）
  │     ├─ 某电机无响应 → D2/D3（单电机测试 + 查行程）
  │     └─ 过温过流 → D4（0xAA 限流 + 冷却）
  │
  └─ L515 问题
        ├─ python apps/diag_camera.py               # ★ 深度诊断：版本/枚举/各组合/连续取帧
        │     ├─ 版本错误 → C1（装 2.54.2.5684）
        │     ├─ 拒绝访问 → C2（管理员/隐私权限）
        │     └─ 无设备 → C3（USB3/驱动/供电）
        └─ python apps/test_camera.py --realsense --show
              └─ 启动失败/花屏 → C4/C6/C7（分辨率组合/对齐/可视化）
```

**最常用命令速查：**

```bash
python selfcheck.py                              # 环境自检
python apps/diag_motor.py --method pcan          # ★ 电机深度诊断（行程/找零/故障码）
python apps/diag_motor.py --method pcan --fix-homing   # ★ 自动回零修复（找零告警11）
python apps/test_hand.py --method pcan           # 灵巧手连接性（0/16 排查）
python apps/test_hand.py --method pcan --move    # 附带张开/握拳
python apps/diag_camera.py                       # ★ L515 深度诊断（版本/枚举/组合/取帧）
python apps/test_camera.py --list                # L515 识别（版本/权限/设备）
python apps/test_camera.py --realsense --show    # L515 出图验证
python -m gui.hand_gui                           # GUI（连接测试/滑条/单电机/查行程/回零）
python apps/mimic_demo.py --method pcan --scale 0.3 --hand-lr 1   # 动作模仿（安全起步）
python apps/test_all.py --method pcan            # 综合验证
```

---

## G. 常见症状速查（本次实测）

### G1. "动作几乎看不见 / 行程太小" → 电机找零告警（status=11）
- **现象**：自检显示故障电机如 (9, 11)；电机行程 <3000；动作幅度极小。
- **根因**：**电机未完成回零（找零）**，位置基准未建立，行程受限。
  手册 enret_t：`11 = 电机找零告警`。**这不是行程坏了**，是没回零。
- **解决（三选一）**：
  1. `python apps/diag_motor.py --method pcan --fix-homing`（自动回零 + 复检）；
  2. GUI → 点【回零(homing)】按钮；
  3. 上位机执行归零动作。
- **验证**：回零后 `diag_motor.py` 再跑，行程应恢复 ≈4095，动作幅度正常。
- 若回零后仍告警：检查机械是否卡住、电源是否足够（24V/8A+）。

### G2. "能打开摄像头但有警告/无法读取帧" → Windows 权限（0x80070005）
- **现象**：`test_camera.py --realsense` 能打开设备但 read 返回 None、警告 wait_for_frames 失败。
- **根因**：`MFCreateDeviceSource ... 0x80070005 拒绝访问`——Windows 相机隐私权限或非管理员。
- **解决**：**以管理员身份运行**终端/程序；或 设置→隐私→相机→允许应用访问。
- **验证**：`python apps/diag_camera.py` 步骤2 不再报"拒绝访问"，步骤4 连续取帧 ≥8/10。

---

*本手册依据知识库《RY-H1(16)灵巧手用户使用手册》V1.0.0、官方 demo、README_1.md、L515_driver.py 编写；如与厂家最新资料不一致，以厂家最新文档为准。*
