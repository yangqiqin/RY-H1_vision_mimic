# RY-H1(16) 灵巧手视觉动作模仿工程 —— 修改记录文档

> **工程**：`RY-H1_vision_mimic/`（Windows 版，摄像头 + 灵巧手，无机械臂；原目录名 `rycan_hand_windows/`）
> **配套**：`rycan_vision_hand/`（Ubuntu/SocketCAN 版）
> **依据**：知识库《睿研灵巧的SDK入门指南》V1.01、《RY-H1(16)灵巧手用户使用手册》V1.0.0、
> 官方 demo（`RyHandLibCANII_rs485_pcan_16.py` / `RyHandLibPCAN_windows16.cpp`）、
> `README_1.md`（L515 PyLive Toolkit）、**`lib/L515_driver.py`（用户提供，标准相机参数）**
>
> 本文档按时间顺序记录**每次修改的内容、改法、逻辑与验证**，供回溯与排查。

---

## 目录

1. [修改1：路径引导（子目录运行找不到根目录资源）](#修改1路径引导子目录运行找不到根目录资源)
2. [修改2：L515 驱动（版本 + 分辨率 + 权限）](#修改2l515-驱动版本--分辨率--权限)
3. [修改3：侧摆安全（GUI 滑条范围）](#修改3侧摆安全gui-滑条范围)
4. [修改4：标准预设动作（握拳/放松）](#修改4标准预设动作握拳放松)
5. [修改5：手型切换（近节/侧摆错乱）](#修改5手型切换近节侧摆错乱)
6. [修改6：L515 标准参数对齐 L515_driver.py](#修改6l515-标准参数对齐-l515_driverpy)
7. [修改7：单电机测试与行程检查（排查电机无响应）](#修改7单电机测试与行程检查排查电机无响应)
8. [修改8：握拳被误判丢弃导致卡住 + 内外展跳变（结合深度相机）](#修改8握拳被误判丢弃导致卡住--拇指内外展跳变太快结合深度相机)
9. [修改9：生成代码注释详解文档（docs/代码详解/）](#修改9生成代码注释详解文档docs代码详解)
10. [控制模型汇总](#控制模型汇总)

---

## 修改1：路径引导（子目录运行找不到根目录资源）

### 现象
直接运行 `python apps/test_hand.py` 时找不到根目录的 `hand/camera/vision` 包，
也找不到根目录的 `RyhandLibx64.dll` / `lib/PCANBasic.py` / `ControlCAN.dll`。

### 原因
Python 直接运行脚本时，`sys.path[0]` 是**脚本所在目录**（`apps/`），不是项目根目录；
DLL/驱动文件是相对路径/依赖 sys.path，解析不到根目录。

### 修改内容
新增 `_pathsetup.py`（根目录），并在所有入口文件顶部加入统一引导：

```python
from __future__ import annotations
# ---- 路径引导（必须最先执行） ----
import os, sys
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
os.chdir(sys.path[0])
try:
    sys.stdout.reconfigure(encoding="utf-8", errors="replace")
    sys.stderr.reconfigure(encoding="utf-8", errors="replace")
except Exception:
    pass
```

入口文件：`apps/test_hand.py`、`apps/test_camera.py`、`apps/mimic_demo.py`、
`apps/test_all.py`、`gui/hand_gui.py`、`selfcheck.py`。

同时加固：
- `hand/hand_controller.py::_load_lib`：DLL 搜索目录 = 「当前目录 + 项目根目录」；
- `hand/transport.py::_ensure_driver_path`：`import PCANBasic/ControlCAN` 前把根目录注入 sys.path。

### 逻辑
1. `sys.path.insert(0, 根目录)` → `from hand import ...` 生效；
2. `os.chdir(根目录)` → DLL/驱动的相对路径查找生效；
3. UTF-8 重配置 → Windows GBK 控制台不报 `UnicodeEncodeError`（中文/✓✗）。

### 验证
`cd apps && python test_hand.py --help` / `test_camera.py --help` / `mimic_demo.py --help` 均 exit=0；
从 `C:\` 任意目录运行 `selfcheck.py` 能找到根目录 hand 包。

---

## 修改2：L515 驱动（版本 + 分辨率 + 权限）

### 现象
`pyrealsense2` 安装正确但仍"识别不到 L515"。

### 原因（两个根因）
1. **版本**：L515 是 Intel 已停产的 LiDAR，**librealsense 2.55+ 已移除其支持**，
   必须 `pyrealsense2==2.54.2.5684`（README_1.md 指明 Just Stable）；
   且 pyrealsense2 2.54 的 `__init__.py` **没有 `__version__` 属性**（旧代码误报"版本 unknown"）。
2. **权限**：Windows 下 `MFCreateDeviceSource` 返回 `0x80070005 拒绝访问`——
   相机隐私权限或非管理员运行，导致设备枚举失败。

### 修改内容
`camera/camera_module.py`：
- 新增 `L515_PYREALSENSE_VERSION = "2.54.2.5684"` 常量；
- `_get_pyrealsense_version()`：优先用 `importlib.metadata.version("pyrealsense2")`
  （修复 2.54 无 `__version__` 误报）；
- `_warn_if_wrong_version()`：非 2.54.x 直接告警；
- `check_realsense_devices()` / `_open_realsense()`：设备枚举异常捕获 + `list()` 立即物化；
  检测到 `0x80070005` 时提示"以管理员身份运行 / 设置→隐私→相机"；
- `_open_realsense()`：版本不符直接抛明确错误（不再静默）。

`lib/requirements.txt`：`pyrealsense2==2.54.2.5684`。
`selfcheck.py`：版本检查改用 pip 元数据。

### 逻辑
版本是 L515 兼容性的第一道闸（2.55+ 直接不认设备）；权限是第二道（枚举被拦截）。
两者都给出**可执行的提示**而不是含糊的"未找到设备"。

### 验证
本机 pyrealsense2 2.54.2.5684 正确判定为兼容；`check_realsense_devices()` 不崩溃；
权限提示逻辑（0x80070005 → 管理员提示）OK。

---

## 修改3：侧摆安全（GUI 滑条范围）

### 现象
GUI 中侧摆滑条**只能往一个方向**（无法给负角度），且可拉到 90°（远超手册 ±20°），危险。

### 原因
GUI 所有 16 个滑条都是 `from_=0, to=90`，侧摆关节（1,4,7,10,13）无法负向且上限过大。

### 修改内容
`gui/hand_gui.py` 新增 `slider_range_for_joint(i)`，按手册角度范围设置滑条：

| 关节类型 | 角度范围 | 关节 ID |
|---|---|---|
| 侧摆 | **-20° ~ +20°**（对称双向） | 1,4,7,10,13 |
| 近节弯曲 | 0° ~ 90° | 2,5,8,11,14 |
| 远节弯曲 | 0° ~ 75° | 3,6,9,12,15 |
| 第16关节 | 0° ~ 110° | 16 |

### 逻辑
滑条范围 = 手册关节角度范围；即使越界输入，`angles_to_motor_cmds` 内部也有 clamp 兜底
（侧摆 90° 输入会被限制为 20° 效果）。

### 验证
- 侧摆 +20°→`[2502,1592]`、-20°→`[1592,2502]`（双向对称）；
- 越界 clamp：侧摆 90°==20° 效果、远节 80°==75°、第16关节 130°==110°。

---

## 修改4：标准预设动作（握拳/放松）

### 现象
做"握手/握拳"动作时，**食指一直伸直、有手指带侧摆**（动作不标准）。

### 原因
复制的官方 demo `palm_bend` 预设本身：食指远节=0°（伸直）、食指侧摆=10°（带侧摆），
不符合"握拳"语义。

### 修改内容
`hand/hand_controller.py::close_hand()` / `relax()` 重设计为**标准动作**：

| 手指 | 侧摆 | 近节 | 远节 |
|---|---|---|---|
| 拇指 | 0° | 50° | 30° |
| 食指/中指/无名指/小指 | 0° | 70° | 60° |

`relax()`：拇指 30/15，四指 35/25，侧摆全 0。
`gui/hand_gui.py::_preset_close/_preset_relax` 同步滑条显示。

### 逻辑
握拳 = 所有手指近节+远节都弯曲、侧摆全 0（握拳不需要侧摆）；
角度均在手册范围内（近节≤90°、远节≤75°）。

### 验证
握拳换算：拇指 `[2275,2275,1637]`（50°/30°），四指 `[3185,3185,3275]`（70°/60°）；
侧摆无差模（M1==M2）。

---

## 修改5：手型切换（近节/侧摆错乱）

### 现象
GUI 滑"11号（无名指近节）"实际动的是**侧摆**。

### 原因
官方 README："**左手 (hand_lr=0)：每个手指的 M1/M2 电机数据交换**"。
左右手硬件上电机 1、2 安装方向镜像，**共模(近节)/差模(侧摆)的物理含义互换**。
手是左手但 GUI 默认右手(hand_lr=1)时，近节指令(共模)在左手硬件上表现为侧摆。

### 修改内容
- `hand/hand_controller.py`：
  - 新增 `set_hand_lr(hand_lr)` 运行时切换（无需重连）；
  - `open()` 拆出 `_init_bus()`；
  - 新增 `move_motor(motor_id, position)` 单电机指令（供排查）；
- `gui/hand_gui.py`：
  - 手型 RadioButton 绑定 `command=_on_hand_lr_change`，已连接时立即 `set_hand_lr`；
  - 状态栏显示当前手型。

### 逻辑
手型只影响 M1/M2 交换（cmds[base] ↔ cmds[base+1]），M3（远节）与手型无关；
切换手型后 `angles_to_motor_cmds` 的交换逻辑立即改变，近节/侧摆物理行为互换。

### 验证
无名指近节45°：右手 → 电机10/11 均 2047（共模无差模）；左手 → M1/M2 交换；
`set_hand_lr(0/1)` 运行时切换 OK。

---

## 修改6：L515 标准参数对齐 L515_driver.py

### 现象
用户提供 `lib/L515_driver.py`（可驱动相机的标准代码），要求按它的参数。

### 标准参数（L515_driver.py）
```
WIDTH = 1280, HEIGHT = 720        # RGB 彩色
DEPTH_WIDTH = 1024, DEPTH_HEIGHT = 768   # 深度
FPS = 30
config.enable_stream(rs.stream.color, WIDTH, HEIGHT, rs.format.bgr8, FPS)
config.enable_stream(rs.stream.depth, DEPTH_WIDTH, DEPTH_HEIGHT, rs.format.z16, FPS)
align = rs.align(rs.stream.color)   # 深度对齐到彩色
depth_cm = cv2.applyColorMap(cv2.convertScaleAbs(depth, alpha=0.03), cv2.COLORMAP_JET)
```

### 修改内容
`camera/camera_module.py`：
- 默认参数改为 **RGB 1280×720 + 深度 1024×768 @30FPS**；
- `_open_realsense()`：启动后创建 `self._align = rs.align(rs.stream.color)`；
- `read()`：`frames = self._align.process(frames)` 深度对齐到彩色，
  并对齐后校验深度与 RGB 尺寸一致（不一致告警）；
- `apps/test_camera.py`：深度可视化改用 `cv2.convertScaleAbs(depth, alpha=0.03)`
  （L515_driver.py 标准）；
- `apps/mimic_demo.py` / `apps/test_all.py` / `apps/test_camera.py`：
  `--realsense` 用 depth 1024×768。

### 逻辑
L515 深度流与彩色流分辨率不同，必须分开配置；`rs.align` 保证深度与 RGB 像素对齐
（眼在手上/像素定位的**前提**）；`convertScaleAbs(alpha=0.03)` 是 L515 标准深度可视化。

### 验证
`CameraModule.__init__` 默认 width=1280/height=720/depth 1024×768（与 L515_driver.py 一致）。

---

## 修改7：单电机测试与行程检查（排查电机无响应）

### 现象
GUI 滑"9号（中指远节）"无响应。

### 分析
换算链路已验证正确（关节9→仅电机9=2457），因此无响应通常是**硬件/行程**问题：
- 电机 ID 映射：中指电机 [7,8,9]，关节9=中指远节由电机9(M3)控制；
- 手册有 `RyParam_GetStroke`（读取行程）：行程过小(<3000)会导致幅度小/无响应。

### 修改内容
- `hand/hand_controller.py`：新增 `move_motor(motor_id, position)` 单电机位置指令；
- `gui/hand_gui.py`：底部新增**单电机测试面板**：
  - `ID` 输入（默认 9）+ `位置` 输入（默认 2048）+ `测试电机` 按钮 → `move_motor` 后回读状态；
  - `查行程` 按钮 → `get_stroke(mid)` 显示行程（应≈4095，<3000 提示行程偏小）。

### 逻辑
用"单电机测试"绕过关节换算，直接对电机发位置指令：
- 若电机9有动作 → 关节换算/手型问题（检查 hand_lr）；
- 若电机9仍无动作 → 硬件/行程问题（查行程、复位、查接线/电源）。

### 验证
`move_motor` / `get_stroke` 方法存在；GUI 编译通过；换算链路（关节9→电机9）验证正确。

---

## 修改8：握拳被误判丢弃导致卡住 + 拇指内外展跳变太快（结合深度相机）

### 现象
- 丢弃率高达 70%+，尤其是**展示握拳动作时**：MediaPipe 识别一直在抖动，
  后处理把帧判为异常丢弃 → 输出不变 → 灵巧手一到握拳就卡住；
- 拇指内外展（关节16，0~110°）能变化了，但**跳变太快**。

### 根因
- `vision/postprocess.py` 旧版用"整帧跳变检测"：任一关节单帧跳变 > `max_jump_deg=20°`
  即整帧丢弃并返回上一帧。握拳是 0→70° 的真实快速运动，加上 MediaPipe 抖动，
  单帧跳变轻松超过 20° → 被误判为"误检帧" → 输出冻结；
- 关节16 范围 0~110°，旧限速 `max_delta_deg=6°/帧` 对该通道过粗，跳变明显。

### 修改内容
1. **`vision/postprocess.py`**（核心重写）：
   - 异常检测改为**方向一致性**：真实握拳是四指协调变化（跳变中位数也大，
     max/median 比值≈1）；只有**孤立单指突变**（某关节跳变远大于其它关节，
     `max_jump > max_jump_deg` 且 `> jump_ratio × 中位跳变`）才判为异常；
   - 异常帧**不再整帧丢弃**，改为"中值替代"（保持运动趋势，只抑制孤立毛刺）；
   - **分通道限速**：`abduct_max_delta_deg`（默认 3.0°/帧）专用于关节16，
     其它关节用 `max_delta_deg`（默认 8.0°/帧）；
   - `update()` 新增可选参数 `fist_confidence`：**深度确认握拳（≥0.6）时跳过
     异常判定** —— 结合深度相机后，真实握拳绝对不卡住；
   - 新参数：`max_jump_deg=40.0`、`jump_ratio_threshold=3.0`、
     `abduct_max_delta_deg=3.0`，均可 `update_params` 运行时调整。
2. **`vision/hand_pose.py`**（深度辅助握拳置信度）：
   - `HandResult` 新增 `fist_confidence` 字段（0~1，无深度为 None）；
   - 新增 `_compute_fist_confidence(pts_norm, real_pts)`：用**深度图 3D 坐标**
     计算四指指尖到掌心中心距离 / 半掌宽（palm_w）的比值，
     张开手 ratio≈2~3 → 0，握拳 ratio≈0.7~1.1 → 1.0；
     线性映射区间 [0.9, 1.6]（ratio≤0.9 握拳、≥1.6 张开）。
3. **`gui/main_gui.py`**：
   - PARAM_CONFIG 新增 `abduct_max_delta`（内外展限速，滑条 0.5~8.0，默认 3.0），
     放在"拇指内外展校准"组；`_apply_calib` 同步到 postprocess；
   - `post` 初始化改为 `max_delta_deg=8.0, abduct_max_delta_deg=3.0,
     max_jump_deg=40.0, jump_ratio_threshold=3.0`；
   - `_mimic_apply(angles_deg, fist_confidence)`：把深度握拳置信度传入
     `post.update(avg_deg, fist_confidence=...)`；
   - 信息区显示 `握拳置信(深度): x.xx`。

### 逻辑
- **方向一致性**：真实运动是"协调"的（多通道同向大跳变），异常是"孤立"的
  （单通道突变）——用 max/median 比值天然区分二者，握拳不再被丢；
- **深度辅助**：L515 对齐深度给出的指尖 3D 位置比 MediaPipe 2D 抖动稳，
  指尖到掌心距离 < 半掌宽 ⇒ 确认握拳 ⇒ 豁免异常判定（双保险）；
- **分通道限速**：关节16 单帧最多变 3°，视觉上平滑跟手，不会猛跳。

### 验证
- `python vision/postprocess.py` 自测全部通过：
  - 静止+噪声：std 1.70°→0.56°（3.1x）；
  - 孤立异常（单通道+50°）：std 1.13°→0.43°，异常帧被识别；
  - **握拳运动 0→70°：40 帧 0 冻结帧（旧版会卡住）**；
  - **内外展 0→110°：最大单帧步进 3.00°（限速生效）**；
  - **深度确认握拳（fist_confidence=0.9）：40 帧 0 冻结、0 丢弃**；
- `_compute_fist_confidence` 单测：张开(0.11m)→0.00、半握(0.06m)→0.14、
  握拳(0.045m)→0.68、紧握(0.03m)→1.00；无深度/NaN→None；
- 28 个 .py 全部编译通过。

### 使用建议
- 使用 **L515 深度模式**（默认 RGB+深度）即可自动启用深度辅助握拳判定；
- 若仍觉得内外展跳，调小"内外展限速°/帧"（如 1.5~2.0）；
- 若握拳跟手偏慢，可适当调大"限速°/帧"（max_delta）或"跟手度(1€)" beta。

---

## 修改9：生成代码注释详解文档（docs/代码详解/）

### 目的
让工程"不读源码也能看懂"：对核心链路 10 个代码文件生成**逐函数、有逻辑**的中文注释文档，
并配套一份架构总览，把模块之间的数据流、控制模型、校准参数串成完整链路。

### 修改内容
新增 `docs/代码详解/` 目录，共 12 份 Markdown：

| 文档 | 详解对象 | 行数 |
|---|---|---|
| `00_总览详解.md` | 整个工程（架构/数据流/控制模型/模块依赖/校准全景/安全/FAQ） | 291 |
| `01_hand_config.md` | `hand/hand_config.py`（配置单点） | 581 |
| `02_angles2motor.md` | `hand/angles2motor.py`（角度↔电机换算，含共模/差模推导） | 578 |
| `03_hand_controller.md` | `hand/hand_controller.py`（ctypes 控制器，含位域解析/线程模型） | 938 |
| `04_transport.md` | `hand/transport.py`（PCAN/CANII/RS485 传输层） | 747 |
| `05_camera_module.md` | `camera/camera_module.py`（相机标准版，针孔模型推导） | 663 |
| `06_camera_lib1.md` | `lib/camera_lib1.py`（相机增强版，时间戳/3D 批量转换） | 668 |
| `07_hand_pose.md` | `vision/hand_pose.py`（姿态估计，内外展 v3/深度握拳置信度） | 1118 |
| `08_postprocess.md` | `vision/postprocess.py`（精度后处理，OneEuro/方向一致性/限速） | 643 |
| `09_main_gui.md` | `gui/main_gui.py`（主界面，线程模型/视频 cover 算法/校准接线） | 1358 |
| `10_pathsetup.md` | `lib/_pathsetup.py`（路径引导） | 586 |
| `README.md` | 文档索引与阅读顺序 | 35 |

### 逻辑
- 每份文件详解做到"不读源码即可完全理解"：覆盖每个类/函数/常量，含公式推导、
  ASCII 数据流图、数字示例、与其它模块的调用关系、常见问题排查；
- 总览文档按"数据从哪来到哪去"组织：摄像头 → 姿态 → 后处理 → 换算 → 控制器 → 灵巧手，
  并给出 16 关节控制模型、校准参数全景表、安全规范、FAQ 速查。

### 验证
- 全部文档 UTF-8 无乱码；内容与源码逐行核对（行数、公式、参数默认值、GUI 滑条范围均一致）；
- 生成方式：10 个文件详解由并行子代理分别撰写，总览与索引由主进程撰写后统一校验。

---

## 控制模型汇总

| 项 | 值 | 依据 |
|---|---|---|
| 关节 ID 1~16 | 拇指侧摆/近节/远节、食指…、小指远节、第16关节(0~110°) | demo 注释 |
| 换算 | `M1=k(θ1/2+θ2)`、`M2=k(−θ1/2+θ2)`(k=4095/90)、`M3=θ3·(4095/75)`、`M16=θ16·(4095/110)` | 手册 + demo |
| 手型 | `hand_lr=1` 右手；`0` 左手每指 M1/M2 交换 | README |
| 初始化 | hook=40、listen=33、32 个 AddListen（16×0xA0+16×0xAA，ID=伺服ID+256） | demo |
| 指令 | 0xAA 位置速度电流（带载推荐）/ 0xA1 / 0xA0 | 手册 |
| 应答 | ID = 发送 ID + 0x100 | 手册 |
| 电机映射 | thumb:1-3, index:4-6, middle:7-9, ring:10-12, pinky:13-15, 16 | README |
| L515 | `pyrealsense2==2.54.2.5684`；RGB 1280×720 + depth 1024×768；rs.align 对齐 | README_1 + L515_driver.py |

---

*本记录基于知识库文档与官方 demo 逐项核对；如与厂家最新固件/库不一致，以厂家最新资料为准。*
